Repair only the reference solution source code. Do not regenerate the task, description,
constraints, concepts, test cases, or expected outputs.

Task title: %s
Task description: %s
Constraints: %s

REPAIR ATTEMPT %d
The previous reference solution failed deterministic execution.
The exact compiler or runtime error was:
%s

Previous reference source:
%s

Return only the complete corrected Java source as JSON matching the supplied schema:
{"sourceCodeLines":["line 1","line 2"]}

The source must be saved as Main.java and run with `javac Main.java` followed by
`java Main`. It must contain exactly `public class Main`, a complete
`public static void main(String[] args)`, no package declaration, and all required
imports before the class. If a symbol is missing, add its exact explicit import.
If parsing failed, close every method, conditional, loop, and class. If input parsing
failed, make the parser match the existing test input; do not change the test input.
Return every source line, not a patch, explanation, or partial program.
